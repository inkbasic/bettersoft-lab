info:
  name: Udate Product
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:3000/products/1
  body:
    type: json
    data: |-
      {
        "name": "Product A",
        "price": 20,
        "quantity": 100
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
