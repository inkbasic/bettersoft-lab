info:
  name: Update Category
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:3000/categories/2
  body:
    type: json
    data: |-
      {
        "name": "Accessories"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
